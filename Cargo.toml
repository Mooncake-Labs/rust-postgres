[workspace]
members = [
    "postgres",
    "postgres-derive",
    "postgres-derive-test",
    "postgres-protocol",
    "postgres-replication",
    "postgres-types",
    "tokio-postgres",
]
resolver = "2"

[profile.release]
debug = 2
